// Tailwind config disabled for Material UI project
// Material UI handles its own styling system

import type { Config } from "tailwindcss";

export default {
  // Minimal config since we're using Material UI
  content: [],
  plugins: [],
} satisfies Config;